# Example helper file
def parse_header(line):
    """
    Simple parser that returns the line and an empty dictionary.
    You can extend this for CGI headers or other parsing.
    """
    return line, {}
